---
import BaseLayout from "../layouts/BaseLayout.astro";
import PostCard from "../components/PostCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("posts", ({ data }) =>
  !data.draft && data.category === "Outdoor Sports"
)).sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---
<BaseLayout title="Outdoor Sports — Tech & Trek" description="Climbing, hiking, snowboarding, and gear notes.">
  <h1 class="text-2xl font-bold mb-6">Outdoor Sports</h1>
  {posts.length === 0 ? (
    <p class="text-muted">No posts in Outdoor Sports yet.</p>
  ) : (
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      {posts.map((p) => <PostCard post={p} />)}
    </div>
  )}
</BaseLayout>
